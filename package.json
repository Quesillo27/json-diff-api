{
  "name": "json-diff-api",
  "version": "1.0.0",
  "description": "REST API that compares two JSON objects and returns human-readable differences",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "node --test tests/"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "keywords": ["json", "diff", "api", "compare"],
  "license": "MIT"
}
